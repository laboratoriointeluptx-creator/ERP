# Despliegue

El despliegue se preparara con variables de entorno por ambiente, health checks y CI para typecheck, tests y build. No se conectara a produccion desde scripts locales ni se incluiran credenciales en el repositorio.
